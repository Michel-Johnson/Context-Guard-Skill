#!/usr/bin/env bash
# Security checks run before the existing product/test branch guard.
set -euo pipefail
HOOK_ROOT="$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)"
exec node "$HOOK_ROOT/.github/scripts/security-hooks.mjs" staged
