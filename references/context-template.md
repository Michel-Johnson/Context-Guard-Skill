# Context folder (v1)

```text
.codex/context/
  FIND.md
  FORMAT.md          (this skill’s copy, or write one)
  sessions.jsonl
  bugs/{id}.md
  fixes/{id}.md
  tasks/{id}.md
  map.json
  owns-index.json    (generated)
  cards/             (generated)
  preferences.json
  user-messages.md
  private/           (gitignored)
```

Four stores: sessions, bugs, tasks, map. No Test Hub, no Roadmap HTML, no feature chains.
