# 修 bug 时顺着哪条线走

还是 Harbor 假仓。三种修 bug 的情况，四种读法。
「该拿到」= 这件事真正需要的规矩是否读到；「多读了」= 读进了无关模块。

## 三种情况

- `fix-tunnel`：坏例挂在 WebChat 上，漏转端口要报错写在隧道那张卡；箭头是隧道→WebChat
- `fix-sandbox`：修沙箱逃逸需要身份卡上「密钥不进日志」；箭头是 vault→沙箱
- `fix-session`：这条链上就够；若把 Gateway 每一层邻居都打开，会灌进插件和渠道

## 结果

### fix-tunnel
坏例挂在 WebChat 上，漏转端口要报错写在隧道那张卡；箭头是隧道→WebChat

| 读法 | 该拿的记忆 | 该拿的坏例 | 多读或灌进来的 |
|---|---:|---:|---|
| 只读这条链 | 50% | 100% | R-root |
| 只顺着箭头离开这张卡 | 50% | 100% | R-root |
| 这张卡的往返一跳 | 100% | 100% | R-root |
| 链上每一层都打开邻居 | 100% | 100% | R-gw-only, R-root |

### fix-sandbox
修沙箱逃逸需要身份卡上「密钥不进日志」；箭头是 vault→沙箱

| 读法 | 该拿的记忆 | 该拿的坏例 | 多读或灌进来的 |
|---|---:|---:|---|
| 只读这条链 | 50% | 100% | R-root |
| 只顺着箭头离开这张卡 | 50% | 100% | R-root |
| 这张卡的往返一跳 | 100% | 100% | R-root |
| 链上每一层都打开邻居 | 100% | 100% | R-gw-only, R-root |

### fix-session
这条链上就够；若把 Gateway 每一层邻居都打开，会灌进插件和渠道

| 读法 | 该拿的记忆 | 该拿的坏例 | 多读或灌进来的 |
|---|---:|---:|---|
| 只读这条链 | 100% | 100% | R-root |
| 只顺着箭头离开这张卡 | 100% | 100% | R-root |
| 这张卡的往返一跳 | 100% | 100% | R-root |
| 链上每一层都打开邻居 | 100% | 100% | R-obs-clean, R-root, R-untrusted-in |

## 人话

- 只读上下级链：修 WebChat 隧道问题、修沙箱逃逸，会漏掉隔壁卡上的规矩。
- 只许顺着箭头离开这张卡：箭头若指向这张卡，同样漏。给人看的「谁给谁」不能当成 Agent 只许往一个方向走。
- 这张卡能往返找一跳邻居：上面两题能拿到，多读很少。
- 把链上每一层的邻居都打开：修会话裁剪这种本链就够的事，会灌进插件、渠道、身份等无关规矩。

和讨论稿一致：关系线可以保留「谁给谁」这句话；Agent 加载时当能往返的邻居，而且先只打开**这张卡**的一跳，不要把祖先每一层都铺开。真正「牵到别的模块」以后可以再走下一跳，这次实验还没做按需第二跳。

```bash
python3 tests/eval/harbor_recall.py project
python3 tests/eval/harbor_recall.py eval-graph
```

