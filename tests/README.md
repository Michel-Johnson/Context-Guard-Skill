# tests/

这是**测试分支**上的临时测试。产品分支不带这些文件。

不要假仓、不要 `fixtures/`、不要 OpenClaw/Harbor 评测目录。

| 夹 | 干什么 |
| --- | --- |
| `hands-on/` | 临时上手测试。现在只有安装冒烟。 |
| `local/` | 本机截图、dump，gitignore，不提交 |

写测试时只改这里。不要改工作台、skill、`scripts/` 里的产品命令。
