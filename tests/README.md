# tests/

这是**测试分支**上的测试。产品分支不带这些文件。

假仓可以放在这里。

| 夹 | 干什么 |
| --- | --- |
| `hands-on/` | 上手测试。现在是安装冒烟。 |
| `eval/` | 假仓和检索对照（OpenClaw、Harbor） |
| `local/` | 本机截图、dump，gitignore，不提交 |

写测试时只改这里。不要改工作台、skill、`scripts/` 里的产品命令。
