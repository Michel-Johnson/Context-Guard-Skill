# 底层：每次会话记什么、怎么存、怎么找

跳转说明书：`.codex/context/FIND.md`。这里只记已定事项。

一条记忆挂多张卡，等真遇到再做。

## 已定

三个大类：会话、坏例、地图。另有**任务说明书**（一类活怎么走），和坏例同一套跳法：先索引，再打开一份。不是测试中台的功能链。

坏例内部两层：文件夹只当索引，怎么修和代码路径在 `fixes/`。代码不抄第二份。

经验：跟 bug 的进 `fixes/`；跟某块软件、以后都要遵守的进地图那张卡；同类任务怎么走、用哪些命令进 `tasks/`。会话一行只当目录。

文件类型和跳转见 `FIND.md` 四跳。
