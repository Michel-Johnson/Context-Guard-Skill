# 底层：每次会话记什么、怎么存、怎么找

跳转说明书：`.codex/context/FIND.md`。这里只记已定事项。

一条记忆挂多张卡，等真遇到再做。

## 已定

三个大类：会话、坏例、地图。坏例内部两层：文件夹只当索引，怎么修和代码路径在 `fixes/`。代码不抄第二份。

经验不单独开第四个大类：跟 bug 的进 `fixes/`；跟某块软件、以后都要遵守的进地图那张卡。会话一行只当目录。

文件类型和跳转见 `FIND.md` 三跳。
