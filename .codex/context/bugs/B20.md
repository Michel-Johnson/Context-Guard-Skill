# B20 原生 prompt 会打断看图

- node: M2
- status: open
- 现象: 检查器改标题/记忆或确认建图时弹出浏览器原生对话框，看图被打断。
- 触发: 在工作台里编辑需要输入的字段，或走会调用 `window.prompt` / `confirm` 的路径。
- 根因: 输入和确认没有留在页面里。
- 修复:
- 守卫: 工作台交互不得出现 `window.prompt` / `confirm`；确认和附件路径都在 HTML 里完成。
- 证据: workbench-global.png
