# RULE.md

各平台（Cursor、Codex 等）开发都遵守本文。

## 1. 分支

- 每一次开发都从最新的 `main` 拉取，再开自己的分支。
- 分支名必须带平台前缀：`{平台}/{主题}`。Codex 用 `codex/…`，Cursor 用 `cursor/…`。同一主题可以多平台并行。
- 禁止直推 `main`，一律走 PR。
- PR 合并后自动删除远程分支。
- 合进 `main` 前必须同步最新 `main`。冲突在自己的分支上解，不要改别人平台的 PR。

## 2. 临时文件

- 开发产生的所有临时测试、临时脚本，一律放到仓库根目录的 `temp/`。
- `temp/` 必须写入 `.gitignore`，不能上传到云端，也不能当作正式测试。
- 删除 `temp/` 之后，项目必须仍能正常运行。
- 每一次 PR 合并成功后，删除本地 `temp/`。

## 3. 未测接口

- 没写测试的接口必须注明。
- 仓库根目录建立 `CI_todo.md`（没有就新建）。
- 所有开发完成但没测试的模块，都要写进 `CI_todo.md`。
- `temp/` 里的草稿不算测过。补上正式测试后划掉。

## 4. 源码与开发记忆分离

- 源码、产品文档、正式测试和已批准的 CI/CD 继续按上述分支和 PR 规则进入 GitHub main；不改变禁止直推、合并前同步和测试门禁。
- 整个 `.codex/` 不进入任何 GitHub 分支、PR 附件、CI 产物或 npm/Skill 分发包；不允许改名或复制到其他目录绕过。公开 PR 只写必要的变更与验证摘要，不粘贴私有记忆。
- 本项目所有开发记忆以用户指定的私有服务器为统一来源：main 基线、各 Session、用户消息、任务、Bug/修复、Map 和索引均从服务器按需读取、向服务器归档。现阶段完整保留记忆，不按长期/临时价值剪枝；源码本身仍以 GitHub 为准。
- 本地 `.codex/context/` 只作有版本的缓存、当前工作副本和待同步队列，不能成为另一份权威记忆。每次回复前校验当前 Session 的项目/工作树绑定及服务器记忆版本；未配置、不可达或同步失败时必须说明，不能把本地旧记录当成服务器最新状态。
- 同项目各 worktree 共用服务器项目，但 Session 记忆彼此隔离；All Sessions 只读服务器中与已合并 main 提交对应的基线。Session 上传不等于更新 main；只有确认代码进入主分支后才发布相应基线。没有 main 时由用户确认权威主分支，不能猜测。
- 具体读写、同步确认、断线和迁移规范见 [服务器记忆规范](references/server-memory.md)。规范确认不等于功能已实现；未实现项写入 `CI_todo.md`，未部署前不得宣称已从服务器读取。
- 服务器连接信息只保存在未跟踪的本地配置中；本项目交接位置为 `.codex/context/private/memory-server.md`。`.env`、密钥、真实 dump 不进 git，也不作为项目记忆上传。服务器读写均需鉴权；本机凭据、授权令牌和进程状态不随记忆迁移。密钥扫描不能绕过，开发脚本草稿继续走 `temp/`。

## 5. PR

- 一个 PR 只做一件事。
- 检查没绿不合并。
- 没准备合的 PR 保持 Draft。
- PR 里写清测了什么；没测的指向 `CI_todo.md`。
- 用户已经明确要求“开发、修复、执行或合并”时，该命令就是本次实现与正常交付步骤的授权。计划记录、归档、PR 合并后的本机 Skill 更新和测试不得再次要求用户确认；只有范围仍不明确、需要破坏性操作或需要新增外部权限时才询问。
- PR 合入 `main` 后任务尚未完成。Context Guard 计划必须保持进行中，直到合并后的本机验收完成后再归档并执行 `plan-finish`。必须立即拉取并核对最新 `origin/main`，从包含该合并提交的源码更新本机 Context Guard Skill；不得从已删除的开发分支、旧工作树或缓存安装。
- 更新后必须校验源码与安装副本版本一致，运行 `context-guard doctor`，并通过安装后的入口重跑本次变更的真实功能验收。只运行源码测试或安装拷贝测试不能代替运行验收。
- 最终证据必须包含 main 合并提交、安装来源、版本一致性结果和真实功能输出。任一项失败时不得宣称交付完成；Codex 原生 Hook 要求重新信任时必须如实报告，禁止使用危险的 trust bypass。
