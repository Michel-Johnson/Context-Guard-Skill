# Cursor 界面参考

核对日期：2026-08-30。参考为本机 Cursor 3.17.8，应用提交 `2fdd31c9f33f7fbe501f2d57772dc5bf64b63620`，窗口画布 1280×800。

本轮只读查看已有 Cursor 测试项目窗口和随应用静态资源，没有输入、发送消息或修改项目；查看后恢复原最小化状态。不公开该窗口的截图、会话、个人路径、扩展数据或工作区名称。`demo-project`、文件树和对话仍是预设示例。

| 部位 | 真实参考 | 宣传层处理 |
| --- | --- | --- |
| 空编辑区 | 居中的 Cursor 标志，下方六条紧凑快捷入口 | 标志和入口共同居中；80px 图标框、160px 入口区与当前参考画面对应，移除原 75px 人工下移 |
| 快捷键 | `Ctrl`、`Shift` 等键各自有键帽，`+` 分隔 | 6 条快捷入口、16 个独立键帽，不再写成一串普通文本 |
| 文件树 | 顶部横排活动图标，选中图标有圆角背景；折叠文件夹没有通用文件夹图形 | 保留 176px 侧栏，18px 树行；纠正缩进、扩展图标、README 信息图标与底部 OUTLINE/TIMELINE |
| 标题栏 | 左侧栏/前进后退、面板/聊天/设置、Agents Window、窗口按钮 | 补齐已观察到的核心入口与原生矢量图形；不复制个人管理员标记 |
| 聊天与状态栏 | 紧凑标题页签、输入区与字号，底部远程/问题/通知图标 | Cursor 专属密度校正；不修改 Claude 样式，不复制用户额外扩展状态 |

静态依据：应用 `workbench.desktop.main.css` 中 `editor-group-watermark`、`letterpress`、`watermark-box`、`watermark-item`、`monaco-keybinding-key` 规则，以及 Cursor 暗色主题。参考 CSS 的居中、独立键帽和透明度关系保留；具体尺寸按当前参考窗口的显示密度换算，不将该版本/主题视为所有 Cursor 配置的统一外观。

图形依据：随应用 `codicon.ttf` 中本次可见控件的字形；空编辑区标志为 `cursor` / U+EDD9。README 图标来自 Seti 主题 `_info` / U+E04D，颜色 `#519aba`。`CursorIcon.tsx` 只存对应轮廓的 SVG 路径，不分发整份字体，不用低分辨率截图。上游素材说明及原许可文本见 `public/client-ui-notices.txt`，站点“关于这份交互演示”提供入口。

真实 Cursor 已查看；宣传页面的浏览器访问仍受 URL 安全策略限制，本轮未绕过。构建、静态组件与镜头回归通过不等同于真实页面的像素对齐、文字清晰度或播放观感验收。B41 保持 open 待实际页面核对。
