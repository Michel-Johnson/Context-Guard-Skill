# 宣传页本轮验收记录

## 滚轮驱动的整屏轨道（2026-09-01）

- 用户指定参考 <https://oyomi.de/#print>。实际检查确认其关键行为是全视口章节纵向堆叠、滚轮驱动整条轨道约1秒位移、固定页头/页脚以及按方向进入的内容动画。本页沿用这一交互模型，但保留 Context Guard 的严谨深色视觉，并将轨道时长收至760ms、内容进入680ms。
- 删除底部上一页、下一页和可点击页码；仅保留不可点击的2px进度线。滚轮/触控板纵向增量达到阈值后翻一页，760ms过渡和140ms惯性静默共同形成手势锁；连续两次同一手势实测只从workbench到clients，没有越过memory。下一次明确滚动、向上返回、首页/安装边界均正确。
- 六页不再通过`hidden`瞬间替换，而是位于同一纵向轨道；当前页`data-active`，其余页`inert`与`aria-hidden`。页面 URL、浏览器历史、页头直达、PageUp/PageDown和双语继续共用同一页状态；切换英文后仍停在Debug，返回中文也保持。
- 内嵌工作台在导览状态把真实滚轮消息交给外层，实际在iframe画面上从workbench翻到clients通过；点击“亲自试试”后同一画面内滚轮不再改变`#workbench`，返回演示后恢复外层翻页。输入、文本、选择器保持自身滚轮路径。
- 实际浏览器逐页检查1280×720与390×667共12个最终状态：document始终等于视口、scrollX/Y为0、每次只有一个`data-active`、非当前页均不可交互，六页主体都位于页头下方和进度线上方，无页面按钮残留或水平溢出。轨道中间采样确认transform从0连续过渡到-607px，而非瞬间替换；控制台无error/warn。
- 普通构建452模块，资源`index-CJm5t_0U.js` / `index-CztliiaQ.css`；10项客户端时序/镜头检查通过。滚轮改变只涉及宣传外层与隔离工作台的事件转交，不调用Agent/API、不读写真实项目。

## 按屏翻页与自动取景（2026-09-01）

> 本节记录前一版显式按钮翻页；按钮已由上方滚轮整屏轨道方案替代，布局与产品比例结论继续作为回归基线。

- 用户要求取消长页面滑动，以明确翻页让每个功能自动进入全屏合适位置。站点改为首页、工作台、客户端使用、项目记忆、Debug、安装六个互斥视口页；页头、前后页、页码线、键盘和 URL 片段使用同一状态，浏览器历史可恢复页面。
- 页面宽度同时受可用高度约束。工作台和 Debug 保持真实产品比例；客户端移动选择器包含七个首用章节及接续/Debug两个场景，短屏适配不删除功能。隐藏页带 `hidden`/`inert`，客户端显式冻结时钟；产品 iframe 与页面内按钮、标签、选择和全窗视角不触发外层翻页。
- 实际页面脚本逐页检查 1440×900、1024×768、768×700、390×667、320×568 共30个状态：document宽高始终等于视口，scrollX/Y为0，每次仅一页可见；主体无页头/翻页控件重叠、无水平溢出，隐藏页均为inert。首次发现并修正1024客户端流程列过高、768 Debug单列过高及320工作台内在宽度/客户端短屏边界。
- 中英文 `#clients` 直达、PageUp/PageDown、页头链接、浏览器后退、语言切换保留当前页均通过。交互控件获焦时PageDown不翻页；客户端“查看全窗”后仍显示播放状态，切出客户端后冻结、返回后继续。控制台无error/warn。
- 最终普通构建452模块，资源 `index-Cj_MBCFO.js` / `index-DjHekIyF.css`；10项客户端时序/镜头检查、仓库安全检查与CI smoke通过。npm dry-run仍为14文件，不含site或.github。正式工作台来源SHA256仍为 `91ec27dfce8621136ee0f93b8527d0238870c135121f5c15dac64ca66e6848f5`，无Agent/API或真实项目操作。
- 原真实DPR1.25/2、系统后台恢复和原生空闲输入状态缺口继续保留；本次翻页验收不把这些环境条件记为通过。全程未使用Computer Use或subagent。

## 合入 main 与 README 入口（2026-09-01）

- 用户明确要求网站源码进入main，并在README最顶部放网站链接；英文和中文README第一行分别进入对应语言版本。Pages改为仅main部署，宣传分支/PR只构建，npm范围与版本不变。
- 先合入main现有改动，不覆盖产品功能或隐私防护。构建使用main的真实工作台（SHA256 91ec27dfce8621136ee0f93b8527d0238870c135121f5c15dac64ca66e6848f5），英文首用七章实际完成，四模块、Memory/Idea与最终回复正确；工作台含Settings的新顶栏已截图核对。
- 452模块构建、10项时序/镜头检查、35项既有安全验收及npm smoke通过，主目录原有36项与smoke也通过。npm dry-run仍14文件，无site/output/.codex；本PR相对main不改变prototype、SKILL、package.json或原CI/npm发布工作流。
- 原真实DPR/系统后台与原生空闲输入状态的验收缺口继续保留，不因合入main而记为通过。

## GitHub Pages 发布准备（2026-09-01）

- 用户已明确允许部署；从codex/promotion-site独立发布，不合入main、不发布npm。先前真实DPR1.25/2、系统后台恢复及原生空闲输入状态的缺口继续保留，发布不代表这些项目验收通过。
- 本地正式构建仍为X6xkmezQ/DW3VXfsT，452模块；10项时序/镜头检查及宣传分支npm smoke通过，主目录既有36项与smoke也通过。npm dry-run仍14文件，不含网站。
- 仅site/dist作为Pages产物；site/源码与部署配置进入宣传分支。output/、本地记录及原生取材不上传，67个旧跟踪记录已仅从索引移除并逐文件核对本地SHA保持。

## 当前：等待真实环境完成验收（2026-09-01）

- 上一目标回合属于实际进展：新增原生参考、修正App局部CSS并完成双语回放。本回合仅核验环境，未改变产品或重复测试，不算新的开发进展。
- 当前实际页面仍加载X6xkmezQ，`devicePixelRatio=1`、`visibilityState=visible`；原生窗口1446960为96DPI、可见未最小化但不在前台，不满足只读截图条件。没有正在等待的测试或构建进程。
- 真实DPR/系统后台条件在Logo回合、原生气泡回合和本回合连续未解除；前两回合的其他进展不代表该限制解除。当前已无可继续的安全操作，整体目标按规则标为blocked，不标完成、不缩减12轮范围。
- 恢复条件：提供125%及200%的实际缩放环境、一次可观测的最小化/恢复过程，并让原生App空闲输入状态可供只读截图。已请求先提供125%环境；不擅改系统设置、不伪造DPR/可见性、不切换受限工具绕过。
- 本回合无源码变更、无新增产品坏例、无提交或部署；上一回合的构建与检查证据保持原作用域，不能替代尚缺的真实环境验收。

## 最新：补充原生气泡参考（2026-09-01）

- 原生App同版本26.825.5331.0新实拍（1936×1096，96DPI）补到用户气泡及完整运行中输入栏，私人内容不进入网站。依据同版本CSS将气泡最大宽度88%→70%、内边距12×18→10×16px、圆角18→22px，字号/行高贴合原生画面；从无文字像素校准气泡及输入栏底色。仅修改App局部CSS，不改CLI、镜头时钟、工作台或Logo。
- 英文→中文→英文三次完整首用均到第07章并显示最终记录回复，每次五条用户消息均无横向溢出；单章完成后的聚焦画面已查看。英文首次结束状态是在启动约69秒后读取，不能把读取时刻当作流程时长或帧率样本。
- 证据：忽略目录 `codex-app-native-message-reference.json`、`app-bubble-focus.png`、`app-native-refinement-en-end.png`、`app-native-refinement-zh-end.png`、`app-native-refinement-zh.json`。中文回放期间验收连接超时，恢复后认领原标签，未重新启动该次回放；实际终态完整，不将连接超时报成页面故障。
- TypeScript/Vite、10项既有时序/镜头检查，以及批准的根npm test（36项＋CI smoke）通过。当前资源 `index-X6xkmezQ.js` / `index-DW3VXfsT.css`，452模块；原生工作台SHA256不变，控制台无error/warn。
- 截图脚本现要求目标为前台窗口，以免屏幕捕获读到遮挡它的其他应用；实际非前台条件下已拒绝捕获，不主动激活窗口。第二次非前台截图未查看、未用作参考或公开产物。此项是本地取材保护，不是产品Bug。
- 仍缺真实DPR1.25/2和系统后台恢复：本次再次读取为DPR1/visible，当前浏览器能力明确只有宽高设置；原生App失去前台也没有令演示document变hidden。已请求用户提供125%环境，不改系统设置或伪造可见性。原生空闲发送按钮仍未实拍；没有绘制的展开菜单不新增为验收条件。本goal turn有新增实现与直接证据，目标保持active，未提交推送部署。

## 最新：简化 Logo 与直达定位（2026-09-01）

- 按用户最新要求，Logo 改为规则 C 形弧线加一个琥珀色节点，移除多层轮廓和内部连线。页头、页尾复用同一个 SVG，favicon 同步更新缓存标识 `c-node`；没有增加依赖或更改动画。
- 实际截图确认两种语言下的新 Logo；页头/页尾图片均加载完成，favicon 指向相同资源。当前英文页面脚本为 `index-CUIwEXdA.js`，控制台无 error/warn。
- 顺带复现首次打开 `#clients` 停在页首：去掉补定位逻辑后，导航→等待→截图仍显示 hero；同一窗口恢复逻辑后直接进入客户端区。隔离过程中没有插入 iframe DOM 快照，不以调试读取的 scrollY 单独认定问题。初始 React 布局就绪后补定位，仅在 scrollY 为0时执行，不覆盖恢复的滚动位置、不改变播放器状态。
- 中英文 `#clients`、英文 `#workbench` / `#debug` 及普通导航点击均已截图核验。证据：`output/client-refinement/clients-anchor-before.png`、`clients-anchor-after.png`、`anchor-*.png`、`simple-logo-clients-final.png`。此项是初始直达问题，与历史 iframe 全量快照干扰滚动的观察分开记录。
- TypeScript/Vite 构建通过（452模块；`index-CUIwEXdA.js` / `index-D-ScI8p5.css`）；批准的根 `npm test` 36项及 CI smoke 通过。正式工作台 SHA256 不变，无提交、推送或部署。
- 已回看12轮计划、英文原始证据及真实 App 参考，修正计划文档中的过期“未执行/Browser受阻”状态。真实DPR1.25/2、系统后台恢复及未实拍的原生App状态仍待验证；不将 Logo 调整或本次局部检查算作新一轮全面验收。

## 最新：整站英文版本（2026-08-31）

- 整站保留中文并新增 `EN / 中文`。英文入口 `?lang=en`，中文入口 `?lang=zh`；保留原有版本参数与锚点，优先使用 URL，其次上次选择和浏览器语言。切换时重建演示，避免混合语言的对话历史。
- 已覆盖导航、标题、四端完整故事和短场景、工作台及其示例数据、Debug、安装反馈、帮助、可访问名称和页面元数据。英文示例用户确认 English 后展示 `--language en`；真实项目偏好不变。英文正文14ms/字符，命令28ms；按完整文本长度结算，不提前发送或截断。
- 生成双语工作台，复用原生英文控件与原布局。实际发现并修复根用途残留中文：原 `adoptTree` 不覆盖 purpose，因此初始化示例也要翻译。另规范架构笔记的 CRLF/LF 翻译键。构建检查地图译文完整，正式产品未修改。
- 本次规定 Browser 工具直接允许重载原4187页面；未绕过此前策略。实际核验 App、CLI、Cursor、Claude 的英文七章、四模块地图、Memory/Idea 与最终收尾回复。CLI 中途切端后回到同一进度完成；不将其记成无中断性能样本。
- App 播放中点击全窗继续输入并完成故事，已暂停时切换视角仍暂停。四端八个短场景在减少动态模式下均展示完整英文及相应记录入口；复制请求显示 `Copied`。
- 六个工作台功能逐一跳到末步，等待真实控件执行后的步骤回执：浏览、记忆、关系、四候选建图、提议加入/隐藏、授权均无中文残留。Debug 的完整步骤快照产生英文 `Dormant lessons 1`。此项为减少动态的结果核验，不冒充全部功能的无中断动画性能测试。
- 320/390/768px 响应式检查无外层溢出（内容宽分别305/375/753px，包含浏览器滚动条差异）；导航与章节选择正常。实际默认视口内容宽1314px。保留原生比例，手机全窗口视角的原生文本仍会缩小，不声称等同桌面字号。
- 中文回切恢复中文提示与 `workbench.html`；英文回切恢复英文且保留 `#clients`，重载仍英文。页面主文档可见中文只剩语言切换按钮。安装展示正确 `--platform codex`，复制反馈英文，英文安装文档指向 `README.md#install`。
- 10项时序/镜头检查通过，覆盖两语言×四端×八组对话的完整输入和反馈；工作台导览语法、TypeScript/Vite构建通过。本回合已运行批准的根目录 `npm test`，36项工作台测试及CI smoke通过。
- 普通生产构建452模块，资源 `index-nI02pDcG.js` / `index-D-ScI8p5.css`；正式工作台SHA256仍为 `7b4ab8e3d0f57403140d96c0e364dcc3c4fffb5209a04269b75f333adb2165f6`。新增静态英文工作台不接 API、本地目录或真实会话。
- 证据位于忽略目录 `output/client-refinement/english-validation.json` 与 `english-*.png`，不进入公开产物。当前页面未见控制台 error/warn 或导览报错。历史真实DPR1.25/2、系统后台恢复及未实拍的原生App细节仍不计通过；本轮英文交付不声称历史12轮目标已全部闭环。未提交、推送或部署。

## 最新：脚本截图对照 Codex App（2026-08-31）

- 用户明确要求“自己写脚本截图，不需要 COMPUTER USE”。已编写只读 `output/client-refinement/capture-app-window.ps1`，先枚举目标进程窗口、再显式指定可见非最小化窗口；使用 `CopyFromScreen` 保存实际像素。未激活/移动/缩放窗口，未输入、发送或操作真实项目，也未调用 Computer Use/subagent。
- 同版本26.825.5331.0截图已取得：1936×1096、96 DPI。约240px侧栏、35px系统菜单区、46px会话栏；主题颜色来自无文字像素，48rem内容最大宽度同时核对只读安装包。完整参考、窗口元数据与边界在忽略目录的 `codex-app-native-reference.json`；私人截图仅在OS临时目录。
- 修正：顶部中文系统菜单；侧栏Codex标题、搜索/通知、新对话、项目下任务及账户栏；会话标题与分享工具；输入栏的权限/模型/麦克风位置和运行中停止图形。移除原来的虚构快捷入口排列、错误模型/本地标签。项目、任务和用户全为演示数据，不复制私人文档面板。原生截图未显示的气泡/菜单/空闲状态仍不算实拍核验。
- 固定1280×800演示画布；侧栏232→240px，焦点内容左界同步372→376px。保持768px内容宽与等比镜头，不改七章时钟或全窗播放意图，不改CLI/工作台/图标。
- 检查：9项既有时序/镜头检查通过；最终TypeScript/Vite构建449模块通过，生产资源 `index-BlR9bci1.js` / `index-CuUCEiCI.css`。原工作台SHA256不变。源码复核补齐停止图形的圆形容器与居中，未新增依赖或低价值测试。
- **本版页面实测未完成**：浏览器初次访问4187时服务未运行（连接拒绝），已在原端口启动隐藏预览进程9612，确认监听与启动日志。随后规定Browser工具重载被URL安全策略明确拒绝，未以换端口、浏览器、截图脚本、间接渲染等方式绕过。没有本版点击、网页截图、全窗切换或长流程实际通过证据；旧版本的下方记录不能替代本版验收。
- 下一步：访问允许后实测新版App完整故事、全窗切换、短场景与布局；真实DPR1.25/2、后台恢复仍待合适环境。原“等待用户提供App截图”已解除，不能再沿用。未提交、推送或部署。

## 四客户端12轮优化（2026-08-31历史基线；新状态见上节）

用户已在计划后授权“完成所有计划”。仅宣传工作树；不使用subagent、真实Agent/API、提交或部署。原工作台SHA256保持 `7b4ab8e3d0f57403140d96c0e364dcc3c4fffb5209a04269b75f333adb2165f6`。

### 前置核对

- 当前 Browser runtime 允许认领用户已有4187标签、读取DOM、点击、截图和重载。历史安全拒绝并未绕过；本次直接使用规定的Browser能力成功。初始实际页面JS是 `index-CDE-uohj.js`，并非根据URL猜测缓存。
- 原始可见窗口1083×958、DPR1。读取正式SKILL的语言、L1确认、会话、Bug和工作台契约。
- 桌面安装包 `OpenAI.Codex 26.825.5331.0`，CLI npm版本0.151.0。只读桌面包布局资源、官方文档与CLI开源TUI；未启动真实会话。Computer Use禁止操作当前ChatGPT桌面UI，已请用户补完整App截图作最终视觉核对，其他工作继续。

### 01 入口与安装映射

- 缺口：只有三个客户端、Codex与安装平台强耦合、切端卸载进度。
- 修改：四个展示ID，App/CLI都映射codex平台；已访问客户端保留实例与本端选择，隐藏端显式冻结共用时钟。
- 实际操作：重载 `index-c75cAAO8.js`，四tab可见；依次点击App和CLI，安装区均显示 `--platform codex`；Cursor在第二章暂停→Claude→Cursor后仍在第二章且暂停。构建通过。
- 剩余：新两端完整画面/故事由03–06轮补齐；最终隐藏工作台边界在10轮复查。

### 02 导航与版式

- 修改：选择线与内嵌键盘焦点分开；四tab窄屏容纳；章节及操作字号调整，不新增外围说明，导航156px/列距16px。
- 实际操作：重载 `index-ByUaYS9R.js`，默认窗口截图检查；390×844下document宽375/scrollWidth375，四tab均完整；Cursor按ArrowLeft选择CLI，焦点位于tab内部。切回默认尺寸。
- 剩余：320及其他宽度随12轮全端复查；尚未宣布所有轮次完成。

### 03 Codex App 独立界面（视觉对照未闭环）

- 修改：新增独立1280×800桌面结构、项目/任务侧栏、对话与输入工具栏；移除占位，不套Cursor文件树。修复嵌套 `main` 误受全站布局样式影响。
- 实际操作：App七章、接续/Debug、全窗/聚焦均可运行；已保存桌面和窄屏截图。
- 剩余：本机26.825.5331.0完整窗口截图尚未取得，只读安装包不能替代真实窗口比对。此轮不标为原生视觉验收通过，详见 `docs/codex-reference.md`。

### 04 App 完整流程

- 修改：接通所有七章；第五轮收尾对话从工作台返回App，说明本轮决定和下次入口；不在语言提问后中断。
- 实际操作：`Codex-App-1/2/3.json` 三次完整七章通过，末尾明确回读 `map.json` 和下一步；正式构建另一次通过（38.7秒）。无真实Agent请求。

### 05 CLI 独立画面

- 修改：新增1040×800终端、等宽中文/英文排版、版本/模型/目录头部、命令与输出；没有桌面聊天气泡。0.151.0官方TUI快照为结构依据。
- 实际操作：CLI入口显示独立终端，桌面/窄屏保持1040×800源画布；终端全窗截图已保存。示例目录不是用户目录。

### 06 CLI 完整调用过程

- 修改：先逐字输入 `codex`，再逐字输入 `$context-guard`、显示选择列表、接入需求；`/skills` 作为另一入口收在使用前提中。
- 实际操作：`Codex-CLI-1/2/3.json` 三次七章通过，终端→工作台→终端收尾完整；正式构建另一次38.2秒通过。短场景与复制通过；未运行CLI、Hook或模型API。

### 07 连续历史与前置状态

- 修改：稳定历史消息、同一地图延续；在建图后的同一工作台写记忆，收尾对话不丢失前文。接续示例改为匹配四个L1和工作台下一步。
- 实际操作：四端各三次完整流程共12次成功；快速跨06→03→07→01→02后四端都停在正确语言回复，无串章/场景错误。单章完成暂停。证据 `rapid-single-chapter.json`。

### 08 打字与节奏

- 修改：按真实调用文字长度计算时间，命令28ms/字、正文24ms/字；180ms动作间隔、700–1200ms阅读停留。滚动连续追踪新增文字，避免每个字符重启补间；Unicode不截半个字符。
- 实际操作：12次完整流程约37.8–42.1秒，八个短场景进度均到100%，完整请求可复制；9项时序/镜头回归通过。UI采用预设文字增长，不声称真实Agent吞吐速度。

### 09 镜头与可读性

- 修改：按四端不同会话宽度取景，保留固定内部比例；镜头停止后吸附目标，避免尾差重绘；App/工作台叠化缩至480ms并等待准备完成。
- 实际操作：1440/1280/1024/768/390/320px × 四端共24个布局样本无外层横向溢出，源宽高保持不变。桌面、390、320截图目检；全窗/聚焦状态见10轮。
- 限制：窄屏保持桌面画布比例时文字会缩小，不声称手机与桌面字号相同；当前实测DPR1，未覆盖系统DPR1.25/2。

### 10 播放意图与隐藏事件

- 修改：四客户端实例独立保留；隐藏端时钟停止，工作台迟到完成先保留，回到对应端再接续；全窗只改镜头。
- 实际操作：四端各在暂停/播放中全窗与聚焦往返，16次切换都保留意图（`controls.json`）。Cursor隐藏1.2秒前后输入/回复完全一致，回到Cursor仍播放（`hidden-client.json`）。点击App内工作台节点明确进入手动暂停（`manual-state.txt`）。
- 限制：跨浏览器标签的系统后台/恢复没有单独实测；已验证的是页面内切端和离屏，不将两者混称。

### 11 工作台、短场景与恢复

- 发现并修复：记忆步骤假定 `#nav-crumbs a` 必然存在，叶模块仍在根视图时会停在“未找到产品控件”。现在无面包屑时点击根节点，再选择工作台。四端12次完整建图/记忆证明路径可达。
- 发现并修复：工作台已loaded但场景未prepared时可能一直等待；增加准备超时与统一可见错误出口。
- 实际故障注入：仅临时替换 `dist/generated/workbench.html` 为不握手的静态页；10秒后显示载入超时与重新载入。恢复原产物后实际点击按钮，工作台正常载入并继续到讨论第一层。全程try/finally恢复，未改产品文件。证据 `workbench-load-failed.png` / `workbench-recovered.png`。
- 接续/Debug共8个短场景完成且复制成功（`short-scenarios.json`）；反馈仅记录现象和待确认原因，不伪造修复、通过测试。

### 12 综合验收与发布隔离

- 修改：移除短场景重复三步小字；小屏章节选择框、13px操作/复制文字、复制结果live反馈；清理占位和失用样式。减少动态下完成事件仍须结算章末，已修复进度停在章首的问题。
- 实际操作：手机选择框可跳章，键盘Home/End/ArrowLeft切端有效；四端静态04/06/07共12次进度分别到4/7、6/7、7/7（`reduced-motion.json`）。先前不滚入视野的样本仍在准备是离屏冻结，不作成功证据。当前控制台error/warn为空。
- 性能：QA构建以rAF采样，12次回放平均帧间隔14.17–23.43ms、Long Task计数均0，但部分采样最大间隔达到1626.6ms。测试含浏览器自动操作和宿主调度开销，尚未定位该离群间隔的来源；不声称稳定60FPS或完全没有卡顿。
- 正式构建：`index-CpkpClkp.js` / `index-C_31EjS5.css`；TypeScript/Vite、9项时序/镜头检查、导览语法通过。App/CLI各追加一次正式版本全流程通过，页面无性能采样属性，JS不含采样代码。
- dist共8个文件，所有HEAD200且MIME正确；没有本机包资源、用户截图、私人路径或项目上下文。原工作台SHA256未变；取材/证据 `output/client-refinement/` 已gitignore。未新增依赖，未提交、推送、部署。
- 剩余验收：03轮同版本App完整窗口对照、不同DPR/实际后台恢复，以及性能离群帧的进一步归因。12轮范围已经实施并逐轮记录，但不能报告全部验收闭环或所有Bug清零。

### 证据位置与版本

本节证据均位于工作树 `output/client-refinement/`，不公开发布。12次完整回放和24布局样本对应QA构建 `index-Bged5KfJ.js` / `index-8RwF0oLz.css`；短场景对应 `index-3nNSBTIy.js`；减少动态、恢复和切换补验对应 `index-CsRDpEkJ.js`。正式版本追加回放见 `Codex-App-production.json`、`Codex-CLI-production.json`，全窗图见 `codex-app-production-full.png`、`codex-cli-production-full.png`。不把不同构建的证据混称为同一次验收。

历史记录中的“Browser仍拒绝访问”“Codex仅占位”和旧时序不代表当前版本；图标历史方案仍未获用户认可，本轮未改图标。

### 续轮：性能离群复核与环境能力（2026-08-31）

- 上一目标轮属于实质进展：代码、正式构建和真实浏览器证据均已更新。本轮继续收敛剩余验收，没有重新定义原12轮范围。
- 通过Browser正式能力核对：viewport只接受width/height，没有DPR参数；visibility控制面板展示。实际隐藏面板、再新建另一个空白标签，目标页仍报告 `document.visibilityState="visible"`。恢复原展示状态并关闭临时空白标签；不能把这两次操作算作后台暂停/恢复通过，也不通过修改document.hidden或系统设置伪造覆盖。
- QA采样增加p95/p99、API支持标记、长动画帧/阻塞/渲染时长与长间隔所在章节，且在停止时读取剩余Observer记录。指标来源为浏览器[Long Animation Frames API](https://developer.chrome.com/docs/web-platform/long-animation-frames)，不是用Long Task=0推断没有掉帧。此代码仅QA构建启用。

| 回放条件 | 帧数 | 最大间隔ms | p95 / p99 ms | >50ms | 长动画帧 | 最大阻塞ms |
| --- | ---: | ---: | --- | ---: | ---: | ---: |
| App 无中途操作 | 2933 | 26.7 | 14 / 14 | 0 | 0 | 0 |
| CLI 无中途操作 | 2942 | 53.3 | 14 / 15 | 1 | 0 | 0 |
| Cursor 无中途操作 | 2977 | 26.8 | 14 / 14 | 0 | 0 | 0 |
| Claude 无中途操作 | 2917 | 79.9 | 14 / 27 | 2 | 1 | 0 |
| App 每650ms读取状态 | 2927 | 53.3 | 14 / 14 | 1 | 1 | 0 |

- 五次均实际到“留下记忆”完成暂停。无干预样本对应QA `index-B3XOLgCa.js`；轮询对照对应补齐Observer收尾的 `index-aMA9-etj.js`。原始记录为 `performance-unattended-app.json`、`performance-unattended-Codex-CLI.json`、`performance-unattended-Cursor.json`、`performance-unattended-Claude-Code.json`、`performance-polled-app.json`。
- CLI工具等待超时后连接被重置，重新取得现有页面才读取已经完成的采样，没有因此重启CLI演示或把工具超时当页面错误。Cursor第一次读取得太早，未作为完整样本；重新无干预完整播放后的记录才进入上表。
- 本轮没有复现历史1626.6ms间隔；轮询对照同样未复现，因此不能认定“轮询导致旧异常”。没有可复现的生产代码阻塞依据，不为消除历史数值盲改产品；保留历史证据，若再现先查时间、章节、可见性及长动画帧数据。
- 原有镜头测试现在覆盖App768px会话与CLI988px会话的左右边界和回复增长，9项测试通过；这不代替真实DPR截图。
- 仍未完成：Codex App同版本完整窗口视觉对照，以及真实DPR1.25/2、后台/恢复的环境验收。需要参考图及能提供这些条件的实际环境，不把源代码、模拟标志或更多重复回放当作替代证据。

### 结项阻塞复核（第三个目标轮，2026-08-31）

重新读取现有分支、正式dist、参考清单与性能证据：正式资源仍CpkpClkp/C_31EjS5，上一轮确有采样与守卫更新。用户未补充真实App窗口参考，也未明确切换到Chrome；当前已验证的Browser能力条件未变，没有新的可执行验收项。此前两轮持续存在的同一参考缺口第三轮仍在，目标标记blocked，保留全部成果，不标全部完成。恢复条件：取得完整Codex App截图；用户明确指定Chrome后，再核实其实际可用性并补DPR/后台验收。未修改页面、重复跑回放或部署。

## 最新调整：品牌图标（2026-08-31）

- 将括号横线标记换成 C/G 形守护边界与内部三个地图节点，米白主轮廓、琥珀色根节点；页头/页尾32px与标签页共用 `public/favicon.svg`，更新资源版本参数避免旧图标缓存。未改真实App/工作台标识、交互或动画。
- 用MuPDF直接渲染独立SVG，检查16/24/32/48/128px轮廓、间距与颜色；证据在 `../output/brand-review/sizes.png`，不是页面截图或浏览器集成验收。构建443模块通过，JS `index-B4j3FWjb.js`、CSS `index-C5xBkpHv.css`；主页/JS/CSS/SVG的HEAD/MIME正确，产品源SHA不变。未新增测试/依赖、使用subagent或提交部署，既有Browser限制和B42待验项保持。

## 最新调整：统一删除外围小字（2026-08-31）

- 用户明确不接受“预设流程 · 不执行代码修复”等小字。删除 Debug 标题旁说明、工作台角标、App 演示顶部路径/预设标签、播放栏重复说明、章节计数脚注、导航小标题、记忆卡片路径/底注和安装区默认小字；一并移除失用样式与对应留白。
- 播放栏仅保留当前步骤名称、功能按钮和必要状态反馈；完整预设边界与素材许可仍收在既有折叠说明中。复制反馈保留空的常驻 live region，仅实际有反馈时占用空间。真实 App 字号/布局、产品工作台、时序与播放事件未改。
- TypeScript/Vite443模块构建通过；原七章/导航/历史/同图延续检查通过（正常完成改验最后章与播放状态，不再断言已删除的说明）；原独立片段32次切镜及跳章回归通过。源码扫描无已删除角标/组件样式，产品 SHA256 不变。以上是源码/隔离组件检查，不是浏览器实际操作。
- 预览 `?v=CDE-uohj#clients`；JS `index-CDE-uohj.js`、CSS `index-DPfPN__T.css`，主页/资源/工作台 HEAD200且MIME正确，dist仍8个公开文件。Browser限制及B42等待验项保留；未用subagent，未提交、推送或部署。

## 最新调整：收窄导航、精简小字（2026-08-31）

- 根据导航及两段说明截图，将桌面导航从比例列改为148px，列间距28→20px；保留44px章节点击高度及950/620px断点的网格导航，不挤压或拉伸原生客户端画布。
- 删除“给人和Agent…”和“从语言确认…”两句，以及记忆/Debug/安装的装饰性副标题、页尾重复标语与App长脚注；清理对应CSS。官方使用说明移入折叠前提；保留操作字幕、安装条件/状态、错误、预设演示标识及完整边界/许可说明。
- 源码检查确认旧文案/样式移除，既有七章/跳章/同图延续/导航回归通过；TypeScript/Vite443模块通过。JS `index-ijpHmcRS.js`、CSS `index-C5obZnHU.css`，带版本入口和资源HEAD/MIME正确，产品源SHA不变。未改时序、镜头、播放事件或产品UI，不新增测试框架。
- 浏览器访问限制仍在，实际空间分配、换行和窄屏观感未验证；既有B42等待验项不变。无subagent、提交、推送或部署。

## 最新调整：缩短动作间的空等（2026-08-31）

- 用户反馈每个动作之间停顿过长。时序审计发现：输入完成后仍等1.38–1.98秒才发送，回复完整后仍等3.22–5.24秒。现按请求/回复长度计算时序：输入后180ms发送，记录间180ms，回复尾部停留820–1120ms；首用四轮App合计由37.3秒缩至13.792秒（不含工作台操作和跨画面交接），接续/Debug由各12秒缩为4.34/4.72秒。
- 工作台仅压缩静候：开场2400/1600→650ms，普通步骤1300/1600→450ms，写入内容后800ms，循环前3000→1100ms，候选点击额外间隔240→100ms。保留620ms光标移动、160ms按压、32ms逐字输入、产品1600ms Bug状态计时及1700ms等待沉淀；App命令40ms/文字20ms和680ms交接不变。
- NativeConversation/NativeViewport/播放器/章节时长同源；独立片段的步骤提示使用其自身时间点。链接指针入场缩短且在离章前完成。2,296次实际组件静态采样、6个历史边界通过，额外检查发送前整条请求完整、结果出现前回复完整、末尾阅读时间和指针不被截断。
- 实际播放器/叠化Hook合成时钟24次切镜、暂停/后台/迟到事件，原七章/直达/同图延续、32次独立片段切镜、6项镜头回归通过；实际工作台编排函数的计时/动作替身检查覆盖开场、续播、记忆阅读、循环及Debug等待。以上不是浏览器点击或观感验收。
- 脚本语法、TypeScript/Vite443模块通过；JS `index-CKnRt-6H.js`、CSS `index-B8Bu6WNJ.css`；带版本预览主页和JS/CSS/工作台HEAD200且MIME正确。产品源SHA仍为 `7b4ab8e3d0f57403140d96c0e364dcc3c4fffb5209a04269b75f333adb2165f6`。Browser限制不绕过；节奏观感及B42等既有实际待验项保留，未用subagent或提交部署。

## 最新补修：隐藏工作台暂停事件隔离（2026-08-31）

- 用户再报点击全窗暂停，截图为第1章/全窗/播放按钮，未显示工作台交互中断说明。当前按钮没有pause；**截图精确触发源仍未确认**，不能认定是旧缓存或隐藏事件。
- 新检查直接执行FirstUseJourney、useUsagePlayer与usePaneTransition源码（合成React调度、RAF和可见性，非浏览器）。24次切换时逻辑时钟连续，手动暂停及前后台不改变播放意图；注入隐藏工作台onPause则修复前失败，player.playing变false。补修workbenchPause按当前画面隔离后通过，退场回调也不暂停新App轮次，当前工作台交互暂停/重播仍保持。
- 原七章/直达/同图延续、32次独立片段视角检查和真实NativeViewport Hook通过。TypeScript/Vite443模块通过，资源 `index-DXceU-Do.js` / `index-B8Bu6WNJ.css`；新预览入口 `http://127.0.0.1:4187/Context-Guard-Skill/?v=DXceU-Do#clients`。带构建参数使链接导航重载文档，而非仅跳同页锚点。
- HEAD核对主页/JS/CSS/工作台为正确MIME，JS长度与本地构建匹配；**缺失旧hash也可能返回HTML200，单独状态码不构成资源正确证据**。产品原HTML SHA未变。实际页面点击仍受Browser URL策略限制，B42保持open，未绕过、提交或部署。

## 最新调整：合并场景和章节导航（2026-08-31）

- 用户提供七章横栏和四场景侧栏截图，要求合并。现在由 UsageNavigation 统一显示七个首次使用章节、接续工作、Bug 排查；删除横向重复章节栏及独立工作台场景，打开工作台保留在第三章。播放方式归入同一导航，精简场景说明文字。
- 首次流程拥有当前章状态，不复制到外层；自动接续直接更新导航。外部片段跳入任意章节时带入初始章号，正确初始化对话/前文或工作台步骤区间，并等待 prepared 后运行。两种播放器按需挂载，展开“使用前提”暂停实际正在播放的实例。
- 源码隔离检查通过：原七章/同图延续/重播/单章/减少动态，全窗切换保留播放状态；七个入口和越界回退、单份导航与唯一选中项、工作台区间和准备门控、接续/Debug直达七章、原场景重播；独立片段32次全窗切换。6项镜头回归、6,312次对话静态采样和6个历史边界通过。
- TypeScript/Vite通过（443模块），资源 `index-BTh6Wx8n.js` / `index-B8Bu6WNJ.css`；主页、JS/CSS与工作台HEAD200。dist仍8个公开文件，产品原HTML SHA256仍为 `7b4ab8e3d0f57403140d96c0e364dcc3c4fffb5209a04269b75f333adb2165f6`。没有改故事步骤、镜头算法、真实产品、Agent调用或发布链。
- Browser URL限制仍在，未绕过；以上为实际源码回调/Hook和静态检查，不是浏览器点击或布局验收。窄屏换行、导航观感与既有视觉待验项仍待实际验证；未使用subagent、提交、推送或部署。

## 最新修正：全窗切换保持原播放状态（2026-08-30）

- B42：首用长流程及独立App片段的视角按钮都显式调用了 `player.pause()`；已移除，按钮只改变full状态。镜头过渡仍独立执行，播放中继续播放，已暂停则保持暂停，不seek、不重播、不切章。
- 红绿证据：真实FirstUseJourney回调的隔离检查在修复前以“view toggle changed playback state”失败；修复后播放/暂停各6次切换均通过，七章原流程检查保持通过。独立ClientUsagePlayer覆盖Cursor/Claude、播放/暂停各8次，共32次切换，状态/进度/视角检查通过。
- 真实NativeViewport Hook追加播放中缩到全窗再回聚焦的合成时钟检查：故事时钟持续推进、镜头可收敛；原已暂停切换、反向、后台及固定比例检查保持通过。以上是源码回调/Hook检查，没有绕过Browser限制实际点击页面。
- TypeScript/Vite构建通过（442模块），资源 `index-CLHO5X5x.js` / `index-RuOi4bVA.css`；主页与新资源HEAD200。仅改两个按钮与镜头注释，不改产品源码或播放算法；保留8个公开文件。B42代码逻辑已修正，真实页面点击仍待验；旧视觉待验项不变，无subagent/提交/推送/部署。

## 最新修正：按真实 Cursor 核对外壳细节（2026-08-30）

- 用户截图指出左侧文件树与空编辑区仍不像真实 Cursor，记录 B41 open。使用 computer-use 只读查看本机 Cursor 3.17.8 的既有测试窗口（1280×800），确认空编辑区标志、键帽、文件树与标题栏细节；未输入或发送消息，随后恢复原最小化状态。此操作是客户端界面取材，没有访问受限宣传网页。
- 已修：补齐空编辑区的矢量 Cursor 标志；标志和六条快捷入口共同居中，撤掉原快捷区75px下移和过大行距；16个独立键帽；真实活动栏/扩展/README图标，18px树行、原生缩进与底部区块；标题栏导航、面板、设置、Agents Window 和聊天页签图标。Cursor专属字号和输入区密度也按参考收紧；Claude样式与固定画布/镜头算法保持。
- 来源：核对随应用 `workbench.desktop.main.css`、Cursor暗色主题及可见控件字形。只转换25个必要图形的SVG轮廓，不分发整份字体、用户截图或实际会话。保留Seti/Codicons上游素材许可，入口位于折叠的演示说明；详见 `docs/cursor-reference.md`。
- **验证通过**：真实Cursor窗口已查看；静态React确认6行/16键帽、矢量标志、顶部入口、Claude隔离及不包含参考窗口的私人内容。6项镜头测试、真实Viewport Hook（采用收紧后的合成输入区边界）、七章回调、7,514静态采样与6个历史边界通过。TypeScript/Vite 442模块及既有 `npm test` 通过。
- **产物**：`index-CfF0pxxj.js` / `index-RuOi4bVA.css`；dist新增 `client-ui-notices.txt` 后为8文件。产品源SHA256仍为 `7b4ab8e3d0f57403140d96c0e364dcc3c4fffb5209a04269b75f333adb2165f6`；规范化boot捕获后原两块源码保持。未改变预设故事、调用Agent或写真实项目。
- **仍待验**：宣传页面Browser URL限制未解除，未换浏览器、端口或间接渲染绕过；本轮没有新页面截图或实际播放验收。上述静态/状态检查不能证明像素一致、清晰度、镜头观感或帧率，B41/B40/B39及其他待验项保持open。无subagent、提交、推送或部署。

## 最新修正：恢复原窗口比例，连续上下取景（2026-08-30）

- 用户明确要求真实操作比例，允许镜头上下切换但不能生硬；记录 B40 open。上轮按宣传容器反算客户端高度的处理违反此要求，已撤回；以下 B39 历史节的“高度适配/固定取景”不再作为验收标准。
- Cursor 固定 1280×800、Claude 会话区固定 640×800；移除 `--native-window-height` 与 conversation 高度分支。两种流程共用 NativeViewport，仅等比缩放和平移，输入/回复使用同一倍率，不随回复行数缩放；剪裁实际滚动区内的新消息作为取景目标。
- 新镜头保留位置与速度，归零、发送、回复增长和中途反向只更新目标。自动移动由故事时钟推进；暂停后没有独立计时器补跑，主动全窗切换使用独立短过渡且不推进故事。短片重播不再重挂镜头。像素只在目标端对齐，消除移动/静止切换时约 0.4px 的横向轻晃；此偏移由真实 Hook 隔离检查发现，不是浏览器测量。
- **回归通过**：新增可重复 `npm run test:camera`，6 项覆盖原始比例、长短回复同倍率、单调平移、归零/暂停/反向、30/60/120Hz 及不规则采样、静止收敛。真实 NativeViewport Hook 配合合成控件边界和时钟检查发送上移、下轮下移、全窗/反向/后台、缩窄仍保留原画布、减少动态和订阅清理；不模拟网页加载，不替代实际布局。
- **原流程回归通过**：14 组合 7,514 次静态采样和 6 个对话边界前文一致；真实 FirstUseJourney 回调、叠化 Hook 与滚动函数的隔离检查通过。原有持续聊天、prepared 后交接、同图延续均保留。将几何/运动计算与 DOM 测量分开复用，去掉长短片两套取景分支；不新增运行依赖或真实 API。
- **构建/隔离通过**：TypeScript/Vite 440 模块、既有 `npm test` 通过；最终资源 `index-C1ZsrZ7_.js` / `index-Cu-yAX4a.css`。dist 7 文件；原工作台源 SHA256 仍为 `7b4ab8e3d0f57403140d96c0e364dcc3c4fffb5209a04269b75f333adb2165f6`，规范化 boot 捕获后两块原源码一致；CSP 禁止连接。主页、JS、CSS、工作台与图标 HEAD200。
- **实际观感未验**：Browser URL 安全策略限制未解除、未绕过；真实窗口比例观感、消息跟随、快速切章、叠化、清晰度、窄屏及 FPS 仍待允许的浏览器播放验收。B40/B39/B37 及既有 B32–B36 保持 open；不声称所有跳动已在页面消除。无 subagent、提交、推送或部署，预览仍在原 4187 地址。

## 上轮记录：章节之间不再重置画面（其中比例方案已由 B40 撤回）

- 用户明确反馈“一直在跳跃，不连贯”，截图可见新输入时聊天区域大块空白；记录 B39 open。源码确认换章重挂聊天、直接滚到尚未发送的轮次、旧镜头先收到时钟归零、visibility 立即隐藏旧画面四个问题。B38 属于其他任务，未修改。
- 已调整：聊天组件和消息 key 持续保留，未发送轮次不占高度，不预留整段空白回复；沿播放时钟平滑跟随新增内容。长流程入场后稳定取景，原客户端窗口适配容器高度，让聊天和输入区同时留在画面里；自动换章不再反复追逐输入与回复。独立 12 秒短片的分阶段取景保留。
- 画面交接：离开 App 保留时钟和完整对话；两层画面始终存在，680ms 叠化不再依赖 visibility/CSS transition。工作台协议 4 增加 prepared，先恢复前置状态，过渡结束后才执行动画；加载错误仍可进入原有重载入口。暂停/离屏/后台保留过渡进度，反向切换从当前透明度开始。完整播放进度跨章累计。
- **静态连续性通过**：14 个对话组合共 7,514 个采样；相同字符/阶段标识对应相同静态内容。Cursor/Claude 共 6 个相邻对话边界，去掉“当前轮次”属性后，换章前后已完成聊天的静态 HTML 完全相同；新请求在发送前不存在于聊天流。
- **真实源码隔离检查通过**：过渡 Hook 的准备等待、单调透明度、暂停/恢复、反向和减少动态；滚动函数在章节归零和追加内容时起点保持；镜头在多次归零及发送/回复时保持同一取景；320/620/930/1280 宽度的两客户端窗口高度均适配取景。以上采用虚拟时钟/控件桩，不是浏览器测量。
- **流程检查通过**：真实 FirstUseJourney 回调验证七章接续、单章、重播、跳章、手动恢复及减少动态；新增断言离开 App 不调用 seek，保留 8600ms 的离场对话，聊天组件无章节 key；prepared 前禁止工作台播放。真实 tour 编排隔离验证 prepared 先于动画动作，前置快照无动画，连播地图仅 reset 一次。
- **最终构建与隔离通过**：439 模块；`index-brJQmEe6.js` / `index-BCFc4l-c.css`；既有 `npm test` 通过，dist 仍 7 文件，主页/新资源/工作台/图标 HEAD200。产品源 SHA256 仍为 `7b4ab8e3d0f57403140d96c0e364dcc3c4fffb5209a04269b75f333adb2165f6`，规范化既有 boot 捕获赋值后两块原源码一致，CSP 禁止连接保持。
- **仍需实际视觉验收**：Browser URL 安全限制未解除，未绕过。真实 DOM 滚动、叠化观感、快速切章、窗口高度适配、窄屏和实际帧率尚未验证；不声称所有跳动已在浏览器消除。B39、B37 和既有 B32–B36 保留待验。没有 subagent、真实 Agent 请求、提交、推送或部署。

## 最新调整：首用完整故事与分章播放（2026-08-30）

- 用户证据：语言提问后缺少后续文字，App 与工作台片段割裂；B37 open。新流程在 Cursor / Claude 的“第一次使用”提供七章连播与单章模式。Codex 完整界面参考缺口保留，没有用其他客户端顶替。
- 分镜：调用 Skill → 示例用户回复“用中文记录” → Agent 设置语言、返回工作台入口 → 空白画布 → 回到同一会话讨论第一层 → 选入 M1–M4、定稿并加入 → 在刚建好的工作台模块写决定与下一步。没有调用 Agent、执行命令或声称保存到了真实项目。
- 播放：App 多轮对话保留历史，命令/语言/后续请求均逐字输入；工作台链接展示指针移动和点击。工作台完成事件驱动下一章，不用固定超时切走；正常连播不重建 iframe、不重置地图；跳章恢复前置操作；单章结束暂停，全片到最后停止。手动操作暂停后可重播本章；减少动态可静态逐章查看。
- **针对性检查通过**：真实组件回调的隔离 Hook 检查覆盖七章接续、三轮历史保留、单章停止、跳章、重播、手动操作恢复、隐藏工作台的迟到完成消息与减少动态。真实导览 `run`/步骤映射的隔离执行确认完整工作台链只 reset 一次、记忆延续同一地图、任意跳章恢复 8 步、无效 resume 回退重建；产品动作使用 stub，不能代替控件点击实测。
- **静态检查通过**：Cursor/Claude 14 个对话组合共 7,514 个时间采样、200 个逐字前缀；状态标识相同时静态 HTML 一致，文字均在发送/结果时刻前完成并留出阅读时间。历史保留 3 轮且只有一个输入框。TypeScript/Vite、导览语法与既有 `npm test` 通过。
- **最终产物**：438 模块；`index-BDDZ6zuN.js` / `index-32mynKk1.css`；dist 7 个公开文件。源工作台 SHA256 仍为 `7b4ab8e3d0f57403140d96c0e364dcc3c4fffb5209a04269b75f333adb2165f6`；仅规范化 boot 返回值捕获后原两块源码完全一致，隔离 CSP 保持；主页、工作台、图标和新 JS/CSS 的 HEAD 均 200。握手协议升至 3，避免旧导览资源混用。
- **视觉待验**：Browser URL 安全限制仍未解除，未绕过；以上是源码/状态/静态验证，不是实际点击、镜头、滚动、帧率或窄屏截图验收。B37 与原 B32–B36 保持 open，待允许实际页面验收后复核。初次验证脚本遇到 TypeScript 7 JS API 缺失及遍历器默认参数递归，调整隔离脚本后通过，均非页面运行错误。
- 仅改宣传层与本任务上下文；不使用 subagent，未提交、推送或部署。历史章节关于“首用必须观看者点击语言”与“所有 App 片段都是 12 秒”的说法已被本节替代。

## 最新修正：工作台加载恢复与打字更新（2026-08-30）

- 用户原始证据：工作台截图停在“载入工作台”，同时反馈打字卡顿、页面仍有 Bug；新增 B35/B36。截图的完整触发链及实际帧率尚未在浏览器复现。
- 加载：导览复用原 `boot()` Promise，不再执行第二次初始化；字体等待上限 1 秒。父页 `onLoad` 和可见期间的探测可重新获取就绪状态，10 秒未就绪给出重载入口；启动失败与场景失败分开处理。协议版本及 iframe/场景标识过滤旧资源、旧窗口和迟到消息；重载会更换 iframe 与资源查询参数。
- 切换：章节及重播请求合并为一个场景状态，避免 prop 切换时连发两次重置；从 App 反复进入工作台/Debug 复用 iframe，只重置场景。App 重播同时退出全窗视图，加载未完成或减少动态时禁用不适用的播放控件。
- 打字：工作台用整句单时间轴、同一 Text 节点增量追加和光标定位，避免每字等待的帧取整误差累积；App 时钟脱离外层 React 每帧状态，对话只在字符/阶段变化时更新，静态窗口复用，进度与镜头直接订阅时钟。镜头移动期间不逐帧取整，停稳后才对齐设备像素并停止重复样式写入。
- **源码单元检查通过**：运行真实导览函数和父消息过滤函数的隔离测试，覆盖丢弃首次 loaded 后再握手、boot 拒绝、字体永不就绪、协议不匹配、旧 iframe/旧场景消息、0 字及半句暂停/续播、取消后不再输入、始终使用同一文本节点。测试没有加载产品页面、启动浏览器或 HTTP 监听；不是 UI 验收。
- **静态 React 与镜头检查通过**：Cursor/Claude 共 8 场景、每场景 721 个时间采样；相同更新标识的实际静态 HTML 一致，未漏掉字符/可见状态改变。每段仅 81–110 次对话可见状态变化；镜头函数 721 次采样产生 94 次样式写入，停稳后的额外 120 次采样为 0 次写入。此为函数/静态结果，不能宣称实测 FPS 提升。
- **最终构建与边界通过**：TypeScript/Vite、导览语法、既有 `npm test` 通过；资源 `index-BJqFKmA4.js`/`index-CYbn_eGy.css`。工作台源 SHA256 仍为 `7b4ab8e3d0f57403140d96c0e364dcc3c4fffb5209a04269b75f333adb2165f6`；将生成副本捕获 boot 返回值的一处赋值还原后，原 CSS/脚本两个块逐字匹配。**下方历史“生成副本原脚本完全未改”不再适用于本轮，唯一差异为该 boot 调用赋值。** dist 仍 7 个公开文件，主页/JS/CSS/工作台/图标 HEAD200。
- **浏览器回归仍受阻**：此前 Browser URL 安全策略限制未解除，未换浏览器、端口、录屏或间接渲染绕过。真实加载恢复、连续播放帧率、暂停/重播点击、窄屏及清晰度仍待验；不声称全页 Bug 已清零。B35/B36 保持 open，B32–B34 原待验项保留。
- 本轮主代理独立完成，没有 subagent、真实 Agent 调用、永久测试新增、提交、推送或部署。隔离验证脚本只在进程内运行。

## 最新调整：加快节奏与命令逐字输入（2026-08-30）

- 用户要求：动画更快，`/context-guard` 也必须逐字输入。B34 记录原先命令在 2500ms 整段突现的问题。
- 改动：单段 19.2 秒缩至 12 秒；输入镜头 800ms、反馈镜头 750ms；命令每字 40ms，需求/回复每字 20ms。集中时序常量；命令从空串逐步到 `/`、`/c`、完整命令，再接需求，阶段切换不补入整段文本。
- 交互边界：调用步骤跳到命令开头；建议点击不会跳过未打完的命令；请求完整后才可提前发送。沿用同一可暂停时钟，减少动态仍直接展示结果。未改布局、清晰度方案或真实工作台。
- **已完成静态 React 渲染检查**：8 个 Cursor/Claude 场景逐字检查命令各前缀、阶段衔接、需求拼接、发送禁用/启用、发送后清空、回到 0 的占位文本；相同 elapsed 输出一致，未完成命令的建议回调不跳时。12 份预设调用的命令、需求、读取、回复完成顺序通过。仅在进程内加载源码和静态渲染，未启动浏览器或 HTTP 监听，没有新增永久测试脚本。
- 构建与既有 `npm test` 通过；当前 `index-BHjmp26m.js`/`index-CMjw66qe.css`，4187 主页、新资源、工作台、图标 HEAD200。真实工作台源 SHA256 未变。
- **实时播放仍待验**：Browser URL 策略限制未解除，未绕过；上述静态渲染不证明实际帧率、清晰度或真实点击体验。B34 暂保持 open 待播放复核，B32/B33 原缺口保持。本轮未调用 subagent、未提交推送部署。

## 最新调整：App 演示放大清晰度（2026-08-30，视觉待验）

- 原始证据：用户截图中 Cursor 输入内容、模式和模型文字发虚，记为 B33；这是本轮待验证的具体症状。
- 可信原因：`.native-plane` 在脚本缩放时持续设置 `will-change: transform`。[Chrome 官方渲染说明](https://developer.chrome.com/blog/re-rastering-composite/)指出这会保留固定光栅位图；与本例吻合，但本页没有修改前后渲染对比，不能称 GPU 原因已实测确认。
- 调整：移除常驻 `will-change`，保留脚本逐帧缩放，使文字和 SVG 可以按当前比例重绘；镜头平移对齐设备像素。没有替换为位图、改变客户端布局、放大基础字体或改动场景时序。
- 已验证：TypeScript/Vite 构建与既有 `npm test` 通过；最终 CSS 的 `.native-plane` 不再带缓存提示。当前资源 `index-C9FtOymN.js`、`index-CMjw66qe.css`；4187 主页、JS、CSS、工作台、图标 HEAD 均为 200。真实工作台源 SHA256 未变。
- **尚未验证实际清晰度与帧率**：Browser URL 安全策略限制仍在，未用其他端口、浏览器或间接执行绕过。待在 DPR 1 / 1.25 / 2、窄屏检查输入/反馈、暂停/重播后的文字与图标；重新光栅化可能增加过渡期间的绘制成本。B33 保持 open，不以构建通过替代视觉结论。
- 本轮主代理独立完成；未调用 subagent，未提交、推送或部署。B32 的 Codex 参考缺口保持不变。

## 最新纠正：依据真实客户端界面重建（2026-08-30）

用户否决上一版通用App壳：它的侧栏、布局和输入控件均不是实际客户端。上一版构建通过不能证明界面符合需求。

- 真实参考：本机Cursor 3.17.8窗口与底部输入框、`/`浮层已由主代理实际查看；仅输入过临时检索文本，没有发送消息，随后清空并恢复原最小化窗口。Claude参考官方2026-04-14桌面演示画面，只重建其中会话pane，不宣称全App或所有版本一致。
- 改动：Cursor文件树/编辑区/右Agent三栏、独立Claude会话区；中性客户端配色；输入发出后清空，菜单只浮在输入框上方。教学步骤/语言按钮/复制移到窗口外，不冒充客户端控件。
- 镜头：测量重建控件边界，共用elapsed驱动全窗→输入→反馈，暂停不遗留独立过渡；窄屏全景垂直居中已按坐标计算修正。文件读取与说明/命令分开，不把“待确认语言”等状态写成Read文件结果。
- 本轮验证：最终TypeScript/Vite通过（435模块；JS350.00KB/gzip111.29KB，CSS26.92KB）；既有npm test通过；输入/回复时序与读取目标类型静态检查通过。工作台源SHA256仍为7b4ab8e3d0f57403140d96c0e364dcc3c4fffb5209a04269b75f333adb2165f6。
- **Codex尚未完成**：缺当前完整窗口参考，已经撤下错误通用壳，仅保留已核对的调用文本，并向用户请求侧栏和输入框截图。不使用ChatGPT界面或CLI冒充独立App。
- **页面浏览器验收仍未完成**：此前Browser URL策略限制未解除。本机Cursor是界面参考取材，不是绕过限制检查4187网页；未用其他浏览器/端口/间接执行绕过。新版实际点击、截图、镜头过渡及窄屏可读性仍待验。
- 用户最新约束：不得使用subagent。已核对没有仍在运行的子代理，此后由主代理独立处理，不再创建或调用子代理。
- 未提交、未部署，没有新增Agent请求、测试脚本或产品改动。B32保持open，不能把部分重建当作三个App全部验收通过。

## 历史增补：App 内日常使用（2026-08-30，视觉方案已被否决）

本节替代下方“安装 → 新会话 → 调用”的旧调用区结论。当前主流程为首次使用、接续项目、打开工作台、反馈与排查 Bug；三个客户端共享场景与时钟，入口按各自 App 区分。

- 已核对：Codex App 用自然语言显式调用，不冒充 CLI 菜单；Cursor Agent 以 `/` 选择；Claude 限定桌面 Code 标签的本地会话。来源及产品约束见 README。
- 源码审查：首用语言等待用户点击；复制完整请求并暂停；切换场景/客户端卸载旧时钟；重播清空语言和复制状态；减少动态显示完整反馈；重复进入同一工作台或 Debug 也重新挂载对应演示。接续只查最近日志并按需读取命中项。
- 静态动画检查：12个客户端/场景组合的完整请求均包含 Skill 名称；每个场景的输入在发送前完成，回复在结果入口出现前完成。没有以此代替点击实测。
- 本轮运行：`npm run build`、既有 `npm test` 均通过；431模块，JS341.34KB（gzip109.24KB），CSS29.04KB。原工作台两块CSS/脚本逐字保留，源SHA256仍为7b4ab8e3d0f57403140d96c0e364dcc3c4fffb5209a04269b75f333adb2165f6。
- 预览：4187服务仍运行；主页、工作台、图标、index-D1nZDqll.js与index-4nziwVz9.css的HTTP HEAD均200，dist仍仅7个公开文件。
- **仍未完成浏览器验收**：此前Browser URL安全策略阻止访问，未换端口、浏览器或间接执行绕过。本轮无新点击、剪贴板、视觉或390/320截图通过证据。需实测场景切换/输入选用/发送/复制、首用两种语言、暂停续播、减少动态、重复跳转和窄屏溢出。
- 仅前端预设动画；不调用真实Agent，不执行安装，不写入用户项目。未提交或部署。

## 后续增补：三客户端调用动画（2026-08-30）

本节是已被上方 App 日常使用版本替代的历史记录；下方原有浏览器通过记录也不能当作新区域已通过交互验收。

- 已实现：Codex / Cursor / Claude 可切换的安装、新会话、显式调用、工作台四步流程；命令逐字显示、预设发送、索引读取次序、复制、暂停、重播、跳步、减少动态以及与安装区同步。
- 事实核对：安装目标和 Hook 来自当前安装器、Hook、SKILL.md；调用语法逐一核对三家官方文档，链接见 README。Codex 为 `$context-guard`，Cursor/Claude Code 为 `/context-guard`；工作台手动入口使用 `npx` 与显式项目根目录。
- 源码复核：补齐 tablist 方向键/Home/End；非当前场景 inert 且 aria-hidden；网格叠层保留最高场景高度，避免按内容切换时移动播放栏；复制使用完整字符串而非动画中的半条命令。没有增加客户端 API、命令执行或真实文件读写。
- 已运行：本次 TypeScript/Vite 构建、既有 `npm test` 通过。正式工作台、Skill、CLI 和发布工作流未改。
- **浏览器验收被阻止**：尝试接管/重载既有4187预览时，被 Browser URL 安全策略明确拒绝。未换浏览器、端口或访问方式绕过。新区域的点击、剪贴板、暂停续播、跳转、实际布局与390/320截图均未完成，不标通过。
- 待实测：三客户端分别点完四步，复制实际文本；播放中暂停，间隔后检查文字/进度不动；快速切客户端；键盘标签导航；进入工作台后返回；减少动态；390/320内容换行及clientWidth溢出检查。
- 尚未提交或部署。不要把预设“已发送”理解为真实 Agent 请求结果。

---

日期：2026-08-30。范围是 Cursor 参考调整后的本地宣传页与连续动画，不代表真实 Agent 集成或 GitHub Pages 已上线。此前版本的构建、移动端和产品回归结果不能直接作为本轮最终通过依据。

## 已完成的交互核验

| 范围 | 本轮证据与结果 |
| --- | --- |
| 视觉与真实界面 | 已实际浏览 Cursor 公开官网作排版参考；宣传外层缩短文案、扩大产品演示。舞台中的工作台、控件与字体来自产品，不使用自绘替代面板 |
| 连续导览 | 光标移动、点击反馈、原位逐字输入和镜头取景可见；目标框附着真实控件，没有截图中原先的游离空框 |
| 四张候选 | 选入 M1–M4，经过原生校验完成定稿和本层加入，核验了最终状态。CI/CD 候选被真实校验挡住，因此自动演示不选择它 |
| 输入暂停与续播 | 在已输入部分字符时暂停，内容和编辑焦点保持；续播继续输入，没有因为播放按钮夺焦而丢失输入目标 |
| 提议确认 | 连续加入“工作台”，再返回并隐藏另一张“冷启动”提议；没有把同一提议的加入与隐藏伪装为连续结果 |
| Debug 暂停 | Bug 处于 pending 时暂停，状态计时停止，原页面 CSS 动画也冻结；续播后按原型流程继续 |
| 快速切章与重播 | 快速切换章节并重播，没有旧导览继续输入或操作新场景；预设按当前章节恢复 |
| 自由体验 | 在原画布实际拖拽、缩放，交互由真实工作台响应；导览不会继续干扰手动操作。此结论不包含窄屏容器的手动横向拖动 |
| 安装入口 | Codex、Cursor、Claude 切换对应命令；复制按钮和单条折叠说明已实际操作。没有语言选择演示或安装执行日志 |
| 导航复查 | 排除全量 DOM 快照展开 iframe 的工具干扰后，新页面连续截图正常，滚动位置保持；此前导航跳动未复现，不列为已确认产品 Bug |

## 更正与未覆盖项

- **更正旧记录**：先前“五个主干选入并通过”的结论错误，已撤回。当前验收的是四张有效候选 M1–M4 通过真实校验并加入。
- **输入边界未覆盖**：本轮验证了部分字符输入后的暂停与续播，但没有精准命中刚新增空行、尚未写入任何字符的 0 字符瞬间，不能声称该边界已通过。
- **Bug 结果边界**：处理转休眠来自产品原型的 1600ms 计时，不执行代码修复、测试，也不证明已形成有效修复结论。
- **真实数据边界**：预设响应和页面内改动不等于真实文件落盘；没有测试真实 Agent 调用、本机目录接入或用户项目读写。
- **窄屏问题已修复并复测**：320px 视口在 Windows 纵向滚动条占 15px 时，`body` 的 `min-width: 320px` 曾造成横向溢出。移除后 `innerWidth=320`、`documentElement.clientWidth=305`、`scrollWidth=305`。判断溢出应比较 `scrollWidth` 与 `documentElement.clientWidth`，不能只与 `innerWidth` 比较。
- **移动端测试范围**：使用浏览器视口模拟，不是真机触屏验收。窄屏容器的手动横向拖动没有产生可确认的 `scrollLeft` 变化，因此不计为通过；这与原画布自身拖拽、缩放的已通过核验不同。

## 最终收尾核验

| 检查 | 本轮状态 |
| --- | --- |
| 390px / 320px 最新整体页面 | 已截图核验。390px：`clientWidth=375`、`scrollWidth=375`；320px：`clientWidth=305`、`scrollWidth=305`。输入焦点镜头已调整为最多 1:1，文字可读 |
| 320px 自由体验容器 | 内部 `scrollWidth=1280`，外层仍为 305px；返回演示无错误。未将手动横向拖动计为通过 |
| 最终生产构建与脚本语法 | `npm run build` 与导览 `node --check` 通过；本次资源为 `index-DMG46DH4.js` |
| 产品 CSS / 脚本原块保留 | 从源文件提取两个完整 CSS / JavaScript 块，均在最终 HTML 中逐字保留；源文件未修改 |
| 来源 SHA256 | 最终源文件仍为 `7b4ab8e3d0f57403140d96c0e364dcc3c4fffb5209a04269b75f333adb2165f6`，与已记录基线一致 |
| 既有产品回归 | `npm test` 通过。`dev-complete` CLI 当前 parked，按既有 registry 直接执行测试，没有新建 TestHub |
| 工作流与 npm 打包 | `node .github/scripts/verify-workflows.mjs` 通过；`npm pack --dry-run --json` 仍为 14 个文件，不含 `site/` |
| 静态产物私有文件边界 | 最终 dist 仅7个文件：主页、图标、JS、CSS、沙盒工作台、来源摘要、字体许可；无真实上下文、用户文件或开发依赖目录 |
| GitHub Pages 线上验收 | 未部署，未执行 |

最后还实际点击了“减少动态”，两套播放器均停止；Debug 跳到勾选处理后保持“沉淀中”，手动定位末步显示“休眠经验 1”。浏览器控制台未出现 error/warn，临时视口设置已恢复。

## 来源与隔离设计

产品工作副本基线为 `c9ca7c79`（0.4.4）的 `prototype/workbench.html`。此前记录的源文件摘要为 `7b4ab8e3d0f57403140d96c0e364dcc3c4fffb5209a04269b75f333adb2165f6`；最终以本轮生成的 `source.json` 和实际文件核对为准，不把本地换行格式的摘要硬编码成发布要求。

实现保留产品源文件，构建层附加 I/O 隔离、相同字体的内嵌文件、连续导览及可暂停的原型计时。iframe 只有 `allow-scripts`；CSP 禁止真实网络，地图、记录语言和候选来自内存。没有真实目录选择、文件附件、Agent 调用、修复、测试或安装执行。

产品后续更新时，除了重新构建，还须核对选择器、候选校验、操作顺序与文案。当前改动未提交、未合入、未上线。
