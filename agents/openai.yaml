interface:
  display_name: "Context Guard"
  short_description: "Keep folder-scoped project memory"
  default_prompt: "Use $context-guard. Four stores: sessions, bugs, tasks, map. Jump with FIND.md."
