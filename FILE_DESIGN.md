# 文件系统与 Agent 记忆设计

日期：2026-09-06。性质：设计方向、合成实验结论与待验证建议。

本文是经用户授权公开的产品设计文档，不是原始 Session 记录，不包含私有项目记忆、对话转录、凭据或实验运行时数据。本文不宣称建议已实现，也不直接变更现行存储、权限或发布协议。实施前应对照 [RULE.md](RULE.md)、[服务器记忆规范](references/server-memory.md) 和 [CI_todo.md](CI_todo.md) 单独评审差异。

## 1. 核心问题

文件系统设计的重点，不是先争论 MD、JSON 哪种语言或格式更先进，而是回答：

1. Session 应该记什么，哪些信息用于恢复当前工作，哪些用于后续任务复用？
2. 记忆如何归属于项目职责节点，怎样表达跨模块经验？
3. Agent 不知道目标节点时，如何从需求找到正确记忆？
4. 节点和记忆增长后，如何控制模型往返、上下文体积和检索遗漏？
5. Session 的工作记录如何成为可信的 Main 知识，而不把未验证结论发布为项目事实？

总方向：**以节点组织项目知识，以 Session 承接当次工作，通过 Map 连接检索入口与记忆正文。**

## 2. 用户提出并确认的方向

### 2.1 Session 与 Main 是两套规范

本地 Session 的工作记忆与 Main 的项目记忆不能直接当作同一个库。两者在生命周期、写入职责和检索用途上不同，需要分别设计。

这里的“本地”指 Session 工作副本，不代表新增本地权威来源。按当前项目规范，私有服务器仍是开发记忆的权威，本地仅是有版本的缓存、工作副本或未同步草稿。Session 上传不等于发布 Main 基线。

### 2.2 长期记忆与节点强关联

需求、Bug、经验应与树节点或模块职责关联，而不是只按会话时间堆积为日志。模块组织是记忆归属和索引的基础；跨模块内容也必须能够被发现。

节点绑定和目标定位是两个问题：系统可以已有需求归属，但 Agent 的普通输入不应被默认包含精确节点 ID。明确给出节点时可从该节点开始；不知道时必须能够沿 Map 定位。

### 2.3 Map 是 Main 的组织与导航方式

Session 使用 Main Map 检索项目知识，但不应为了保存本次工作过程，复制一套完整 Main Map 作为自己的主要记忆格式。

这是未来设计方向，不是对现有实现的描述。现有产品存在隔离的 Session Map 和发布流程；迁移到新规范前，需明确如何兼容未发布编辑、授权和历史版本，不能直接删除现有 Session Map。

### 2.4 分级读取，避免默认加载整个记忆库

先读取足以定位职责的导航信息，再读取相关记忆正文。节点分层、模块索引和正文分离，目的在于按需获取，不是层级越多越好。

“一任务一 MD”“一个大 JSON”“分级索引”都是讨论过的选项；具体序列化格式、目录层级、记忆粒度尚未定稿。

### 2.5 用实验选择检索路径

不能预设专用工具一定比 Agent 自己读多个文件更快、更省。应比较不同规模下的完整检索时间、累计输入与输出 token，并检查是否真正取得所需依据。

当前已认可的默认方向：**Agent 沿 Map 自主定位节点，批量读取记忆；专用检索是可选加速能力，不是必经步骤。**

## 3. 已完成的实验

以下都是合成数据实验，不是生产性能承诺。原始脚本、语料和逐轮轨迹保留在本地临时实验目录，未纳入 Git 或公开附件。因此本文件提供协议与结果摘要，不构成可独立复跑的公开 benchmark 包。

### 3.1 物理布局：已知节点和记录 ID

比较并行读取 4 份 MD、读取单节点 JSON 后裁剪、读取全局 JSON 后裁剪。返回内容相同，每组 9 次重复，以下为中位毫秒；固定每节点 32 条、每条正文 2048 bytes。

| 节点数 | 并行读取 4 MD | 单节点 JSON | 全局 JSON |
|---:|---:|---:|---:|
| 8 | 1.519 | 1.409 | 3.853 |
| 32 | 1.864 | 1.948 | 15.240 |
| 128 | 1.976 | 2.025 | 64.823 |

结论仅是：为少量已知记录反复读取并解析全局文件，会产生读取放大。不是“JSON 天生比 MD 慢”。没有清空操作系统缓存，没有比较常驻内存、数据库或流式解析，也没有测 Agent 定位或 token。

### 3.2 已知目标节点的 Agent 检索

模型为 glm-5.3，固定 5 个节点，每节点 16/64/256 条记忆；8 道合成题、3 个规模、3 条路径，共 72 个新会话、275 次模型请求。下表为各路径 24 个会话的平均值，失败开销保留；完整成功要求约定事实和必需依据均满足。

| 路径 | 平均秒 | 平均累计输入 token | 完整成功 |
|---|---:|---:|---:|
| 文件操作与真实 rg | 26.07 | 20,856 | 23/24 |
| 节点索引后读正文 | 26.41 | 18,152 | 22/24 |
| 专用词项查询后读正文 | 20.70 | 8,175 | 23/24 |

此时词项查询有优势，但输入已给出目标节点，部分题目和索引提示较强，每格只运行一次。不能把该结果用于证明未知节点导航也有同样收益。早期使用串行 Node 扫描模拟 grep 的版本因对照不公平作废，不混入上表。

### 3.3 未知目标节点：正式 v4

模型仍为 glm-5.3，通过用户指定的 Anthropic 兼容 API 调用。输入只有自然语言问题、工具定义和根 Map 入口；不提供目标节点、完整树、答案字段名或目标文件路径。

规模为 33、129、513 节点：1 个根 + 4/16/64 个模块，每模块 7 个叶节点；最大边深度为 2，即固定 3 层。每个节点 8 条短记忆，总记录数为 264、1032、4104。规模增长只增加无关模块，已有核心节点和证据保持不变。

四题分别测试通知重试、归档编码、跨组件完成状态、祖先诊断约束。每题每规模每路径重复两次，共 48 个新会话、373 次模型请求；不是 48 道独立问题。配对交换执行顺序，按固定种子打乱测试块，全程串行。

| 路径 | 节点定位 | 记忆读取 |
|---|---|---|
| 自主浏览 | 读浅层 Map，自选子节点、祖先或关联节点 | 列标题/路径，批量读 MD 或使用真实 rg |
| 词项查询 | 先按标签/职责筛选直接邻居，再允许原文回退 | 先排名记忆，再读正文；允许查询后回退列表 |

两者都通过工具访问数据。这里不是“有工具”和“没工具”的比较，而是基础读取操作与专用筛选函数的组合策略比较。

共同约束：只访问已发现节点及已返回路径；支持批量操作和分页；单次返回约 24000 字符上限；每会话最多 12 次模型请求，每次最多 1536 输出 token。词项查询默认返回前 8 条，Map 使用标签/职责权重 3/1，记忆使用标题/摘要/正文权重 3/2/1，没有向量、额外模型或语义重排。

先前 v3 的可选查询工具组经常仍先完整读取 Map，完成 15 个探索会话后单独保留。v4 明确要求查询先于回退，因此也包含策略顺序和接口易用性的影响，不代表所有专用检索系统。

#### 全部尝试的结果

每行 8 个会话。输入为各轮累计输入，按兼容接口字段约定包含缓存读取；不是唯一文档长度。耗时包含模型、网络、工具及最终回答。失败也计入平均开销。

| 节点数 | 方法 | 平均秒 | 中位秒 | 平均输入 token | 平均输出 token | 完整成功 |
|---:|---|---:|---:|---:|---:|---:|
| 33 | 自主浏览 | 42.7 | 36.1 | 25,812 | 1,293 | 7/8 |
| 33 | 词项查询 | 47.1 | 40.0 | 32,569 | 1,651 | 6/8 |
| 129 | 自主浏览 | 39.4 | 34.0 | 23,019 | 916 | 8/8 |
| 129 | 词项查询 | 45.2 | 46.0 | 28,723 | 1,476 | 5/8 |
| 513 | 自主浏览 | 37.2 | 33.7 | 48,721 | 1,134 | 7/8 |
| 513 | 词项查询 | 43.1 | 43.6 | 51,278 | 1,345 | 5/8 |

总体每会话平均：浏览 39.77 秒、32,517 输入、1,115 输出、6.71 次模型调用，完整成功 22/24；查询 45.14 秒、37,523 输入、1,491 输出、8.83 次调用，完整成功 16/24。

#### 只比较同题两条路径都成功的配对

这是有选择偏差的补充结果，不能替代全部尝试与成功率。

| 节点数 | 配对数 | 浏览平均秒 | 查询平均秒 | 浏览平均输入 | 查询平均输入 |
|---:|---:|---:|---:|---:|---:|
| 33 | 6 | 35.4 | 41.0 | 14,715 | 27,739 |
| 129 | 5 | 36.1 | 34.1 | 19,339 | 20,665 |
| 513 | 5 | 28.2 | 36.1 | 33,837 | 38,550 |

129 节点的成功配对中查询平均更快，说明不是每题、每规模都由浏览胜出。但查询没有跨规模稳定优势，无法推出强制切换方法的节点数阈值。

#### 质量、时间与 token 核验

完整成功要求必需事实正确、最终答案完成、未报告不足，且必需正文信息实际出现在返回内容中，引用也完整可追溯。只命中文档标题或引用正确 ID 不算取得依据；rg 返回相关正文行可以算。内容由本次 Codex 事后逐项复核，不是独立人工或双盲评测。

通知、归档两题两组均各 6/6；跨组件题浏览 6/6、查询 3/6；祖先题浏览 4/6、查询 1/6。查询有两个会话 12 轮后没有最终答案，另有引用只看过摘要、未读正文的记录，均保留为失败。

正式 v4 用量：628,683 非缓存输入、1,052,288 缓存读取输入、62,525 输出 token。全部请求返回指定模型，逐轮 usage 与会话汇总一致；没有替换模型、自动重试或接口错误。自动缓存未隔离，不同字段的计价不同，不能将 token 总数视为实际账单。

全部模型请求累计约 2035.43 秒，本地工具执行约 1.85 秒。浏览本地工具平均 72.9 毫秒/会话，查询 4.1 毫秒/会话；查询本身更快却没有让完整任务更快。这里不包括索引构建维护，查询使用内存数据，浏览包含文件 I/O 与 rg 启动。

核验覆盖唯一排程、模型与 usage、脚本哈希、已发现节点/路径权限、查询先于回退、返回资料与语料一致、节点/记忆数量及跨规模共有内容一致。没有独立重建生成器内存对象顺序对应的整体语料哈希，不声称这一项也已通过独立重算。

## 4. 实验带来的经验

1. **不能把局部检索结论外推到完整导航。** 已知节点时查询有优势，未知节点时这套查询策略反而增加了成本。
2. **单次返回少不等于累计输入少。** 查询组返回 Map 平均约 5025 字符，浏览约 8624 字符；查询组却增加了轮次和记忆返回。字符不是 token，对话历史会随模型往返重复进入输入。
3. **在本轮规模中，优先减少模型往返，而非争论毫秒级文件读取。** 但不能外推到远端存储、冷启动或海量语料。
4. **先分清职责层级。** 找到模块不代表找到保存具体规则的叶节点；父节点没有答案也不代表其子树没有答案。
5. **祖先和跨模块约束不能只靠偶然发现。** 两组都漏过祖先规则，应把补齐约束纳入读取流程。
6. **摘要是定位线索，不是正文已验证的替代品。** 最终结论应引用真正读取的依据。
7. **检索工具的交互契约也是性能因素。** 查询组 11/24 会话出现工具错误，浏览 2/24；改词项却沿用旧 cursor、未先查询就回退等错误会增加轮次。
8. **冗余会影响排名。** 本实验常规摘要重复模块职责，泛化词项容易将通用文档排在关键规则之前。这是合成语料和简单词项排序的共同局限。

限制：只有四道独立合成题，模板化干扰、清晰职责、固定树深和短正文。未测试自由全目录 shell、成熟搜索引擎、深树、长历史、冲突或缺失资料。此次同时改变 Map 导航和节点内筛选，不能隔离单一因素，也不证明所有工具劣于直接读取。

## 5. 建议的设计草案

本节是基于讨论和实验的工程建议，尚未确认具体 schema，也没有实施迁移。

### 5.1 逻辑结构与物理布局分开

建议使用稳定节点 ID，通过 parent 和 related 表达职责树与跨模块关系。节点移动或更名不应要求所有记忆路径跟着改变。

树用于归属，关联用于跨模块发现。不要把多模块问题强行复制为多份正文；可指定一个主要归属，并由其他相关节点引用同一记录。

Main 是经发布的知识视图，不必等同于一个越来越大的 JSON 文件。逻辑分层也不要求磁盘目录无限嵌套。

### 5.2 导航、索引和正文各负其责

- Map：保存节点职责、父子/关联关系及索引入口，不堆任务过程与大段经验正文。
- 节点索引：保存记录 ID、标题、类型、状态、适用范围和正文引用等检索信息；摘要应短且有区分度，避免重复模块介绍。
- 记忆正文：保存背景、适用条件、结论、反例、验证依据及来源。不为了凑齐字段生成空泛模板内容。

从一个可读的根入口开始；小型 Map 可保持单文件，需要按层读取时再拆分。能一次批量读取的内容不应强制拆成多次模型决策。

一种候选布局如下，仅表示逻辑相对路径，可映射到私有服务器或其本地缓存，**不是要提交到 GitHub 的记忆目录**：

```text
main/
  map.json
  nodes/
    <node-id>/
      index.json
      memories/
        <memory-id>.md
sessions/
  <session-id>/
    session.json
    working.md
    records/
      <record-id>.md
    candidates/
      <candidate-id>.md
```

JSON 用于结构化关系和可校验元数据，Markdown 用于解释性正文。是否需要所有这些文件、是否另设事件流、Map 如何分片，应随 schema 和实测决定，不提前同时维护多种全量副本。

### 5.3 Session 保存工作连续性，不复制 Main 知识库

建议 Session 记录：任务目标与边界、已确认要求和待验证假设、关联节点、采用的 Main 版本、关键操作与决策、验证结果、未解决事项和恢复所需的下一步。

引用过的 Main 记忆记录其 ID 和版本即可，不默认复制正文为第二份权威。需要冻结证据时采用明确快照或版本引用，不能静默使用变化后的内容冒充旧依据。

建议区分“完整历史归档”和“Agent 当前读取的精简工作视图”。这不是删除历史或只保留所谓高价值记录的授权；现行完整保留规范继续有效。结构化 Session 视图也不替代应保留的完整历史。

凭据、认证令牌和机器运行态不属于可迁移记忆；任何归档或摘要流程都不应将它们写入知识正文。

### 5.4 一份记忆对应一个可复用的结论单元

不建议机械采用“一次任务必然一篇 Main 记忆”：一个任务可能没有新知识，也可能产出多个适用范围不同的结论。

候选类型可包括约束、决策、故障经验和操作方法。正文应回答“何时适用、应该如何处理、为何可信、有什么边界”。相同规则优先维护单一正文，其他任务引用它；过期内容明确标记被替代，保留历史而不继续作为当前默认依据。

### 5.5 发布 Main 是独立动作

建议流程：Session 保存过程和验证证据，形成带来源的知识候选；按权限与发布规则检查归属、适用范围、重复或冲突；确认相关变更进入权威主分支后，发布对应 Main 知识版本。

知识提炼不是历史剪枝。未进入当前 Main 检索视图的 Session 历史仍按现行规范保留。纯知识变更、不涉及代码的发布条件及审批者需要另行约定，不能用“代码未变”绕过权限。

发布与同步应采用版本检查、幂等操作和可核验回执；并发更新或基线过期必须显式处理，不能静默覆盖。具体实现复用现有服务端协议，避免再造第二个写入权威。

### 5.6 默认检索流程

1. 已明确节点时从该节点开始；否则读根 Map，按职责逐层定位，可同时选择多个候选。
2. 读取候选节点的短索引，批量获取相关正文；范围过宽时可用 rg 或查询工具辅助收窄。
3. 显式检查适用的祖先约束，必要时沿关联边补齐跨模块规则；不无条件读取整个祖先子树。
4. 正文与任务条件匹配，且所需约束覆盖完整时才停止；找不到或互相冲突时明确报告。
5. 输出结论连同实际读取的记录 ID 和版本。查询结果不足时允许回退原文，不让排序结果变成访问边界。

可在未来提供按预算批量返回节点索引与适用祖先规则的工具，但它只能算待测优化，不能根据本轮数据宣称必然更快。分页应明确绑定查询条件，改变查询即重置游标；返回截断与未覆盖范围应可见。

## 6. 尚待确定的事项

- Session 最小字段、完整历史与恢复摘要的关系、何时更新工作视图。
- 记忆粒度、跨节点归属、引用与版本模型、失效和替代规则。
- 节点分层标准、职责描述、祖先规则适用范围和覆盖/冲突语义。
- Main 发布权限、纯知识变更的发布条件，以及现有 Session Map 的兼容与迁移。
- Map 与节点索引的分片、生成与维护策略；具体 MD/JSON schema。
- 检索停止条件、预算不足时的降级方式，以及更多真实问题上的质量指标。

下一轮若单独验证 Map 筛选，应保持节点内读取方法不变，避免同时改变两个因素。之后再分别增加树深、每节点历史量、正文长度、职责歧义和缺失/冲突资料；记录全部尝试、成功配对、缓存、轮次与输入/输出，不只比较最快个例。

**当前可采用的设计原则：让结构帮助 Agent 少走弯路，让正文支持批量读取，让发布保证知识可信；不要把复杂检索系统当作文件系统成立的前提。**
