# 待办

环节设计都可以在云端做。前端布局放到后面。

**云端：** 改网页样子、改底层文件、写 Skill、设计 Hook、写测试、发包。不需要你电脑上真连仓库。  
**本地：** 连接仓库、真会话、Hook 真响、装到本机。云端能把代码写好，但连没连上只能在你电脑上点。

---

## 云端

### 现在

- [ ] **能用（今天主线）** — skill 最起码能跑通：init 之后工作台能打开、记坏例写全、命令行看得到这些命令
  - [ ] `record-bad-case` 同时写出 `fixes/{id}.md`（现在索引会指向缺失文件）；没节点时 `unassigned_bugs` 工作台要能看见
  - [ ] init 的空 `map.json`（`root: null`）工作台能加载，不要掉回演示图
  - [ ] `node bin/context-guard-skill.js` 无参数时列出 init / set-language / workbench / record-bad-case
  - [ ] init 骨架补齐：`FORMAT.md`、`owns-index.json`、`tasks-index.json`
- [x] 把待办分成云端 / 本地
- [x] **Skill：云端怎么看工作台、怎么确认**
  - [x] GitHack 只给人看已经推上去的图
  - [x] 人在对话里说改哪里；Agent 改仓库文件再推
  - [x] 不要把整张图贴进对话；图改过跑 `python3 scripts/map_owns.py cards`
- [x] **Skill：模块 / 节点怎么加（云端）** — 对话里商量标题和用途，人点头后再写入 `map.json`
- [ ] **Hook 阶段设计（先设计，真跑放本地）**
  - [x] 已接线：开会话 / 子 Agent 开始 → 建目录；人说话 → 记原话；结束 → 提醒去记会话和坏例
  - [ ] 定还要不要接：用工具前/后（只提醒或刷新复印件，**不要拦住**）
  - [ ] 定还要不要接：会话真正结束时收尾
- [ ] **Bug：云端快存、人快看**
  - [x] Agent 用 `record-bad-case` 写成一条（短卡 + 怎么修 + 挂到模块）
  - [ ] 人打开 GitHack 看未修列表；改状态在对话里说，Agent 改文件
- [ ] **底层文件** — 会话 / 任务怎么写、init 骨架、生成小目录（格式已有，按用补齐）
- [ ] **云端测试** — 只在测试分支改 `tests/`，假仓放 `tests/eval/`
- [ ] **优化前端布局**
  - [x] 检查器和画布之间的竖线可拖动改宽度（双击恢复）
  - [x] 顶栏最右「设置」：主题（现在这版 / 奶油描边 / 淡彩分层 / 无阴影扁平）和中英文；还没定默认主题
  - [x] 手机竖屏：顶栏换行、检查器改底栏可拖高度、触控拖动画布，桌面布局不动
  - [ ] 手机顶栏按钮再收一收（只动 `html.cg-phone`，电脑顶栏不动）。电脑上看手机布局：同一页加 `?phone=1`；`?phone=0` 强制电脑布局。不要另做一份 HTML。关系 / 第一层候选 / 授权 / 已取消 / 左右上下 多半进「设置」；Bug 和设置留在外面。

### 以后
- [ ] 第一次用时真去读仓库、一起商量出图（演示文案可云端改；对着你本机别的仓库要本地）
- [ ] 找东西再快一点
- [ ] 安装和发布（npx、`init`、GitHub 发包）
- [ ] 本仓库地图第二层继续拆细（第一层别重画）

---

## 本地

- [ ] 本机打开工作台（`context-guard workbench` 或 Hook 开会话启动）
- [ ] 工作台「连接仓库」，改完写进磁盘上的 `map.json`
- [ ] 人在画布上改完，同一台电脑上的 Agent 下一轮马上看到
- [ ] Hook 在你电脑上真的开火（开会话、记原话、结束提醒）
- [ ] `npx` 装进本机 Codex / Cursor
- [ ] 对着本机其他项目做第一次建图

---

## 别做

测试网页、功能链、用 hook 卡提交。已经删过的别加回来。
